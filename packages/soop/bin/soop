#!/usr/bin/env node
// Launcher shim for @pleaseai/soop CLI binary.
// Delegates to the platform-specific compiled binary via the launcher.
import('../dist/launcher-cli.mjs').catch((err) => {
  console.error('Failed to load soop launcher:', err.message)
  process.exit(1)
})
