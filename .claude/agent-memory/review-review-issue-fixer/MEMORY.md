# Review Issue Fixer Memory

## Project: rpg (soop please)

### Key Facts
- Package manager: Bun (use `bun run typecheck`, `bun run test`, `bun run lint:fix`)
- TypeScript validation: `bun run typecheck` (runs `tsc --noEmit`), clean exit = no errors
- Test runner: vitest via `bun run test <file>`
- `packages/soop/README.md` is gitignored — it's a build-time copy of root `README.md` (generated by `build:copy-assets`). Fix only root `README.md`.

### Known Pre-existing Test Failures (not caused by our changes)
In `packages/utils/tests/llm.test.ts`:
- `should pass claudeCodeSettings to createClaudeCode` — extra default settings issue
- `should call createClaudeCode with undefined when no settings provided` — same
- `should not pass apiKey to createClaudeCode` — same
- `should throw when structured output is null` — error message mismatch

These 4 tests fail on branch `feat/gemini-3.1-flash-lite-default` before our changes.

### Fix Patterns
- Null/undefined guard for API key: check `this.options.apiKey ?? process.env.GOOGLE_API_KEY`
- Log warnings using `log.warn(...)` from `createLogger(tag)` (consola-based)
- Fall back by mutating `this.options` via spread: `this.options = { ...this.options, useLLM: false }`
- Provider-option guards: check `this.options.provider !== 'google'` before emitting googleSettings
